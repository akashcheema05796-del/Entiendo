# Generated by `ent extract` (L1) — never hand-edited (SPEC.md §12).
/entiendo/graph.json
/entiendo/coverage.json

# Generated by `ent render` (L4) and `ent snapshot` (L3) — runtime data.
/entiendo/render.html
/entiendo/history/events.jsonl

# Generated by `ent eval --tier 1`, `ent bless`, `ent baseline` (Phase 7) — runtime data.
/entiendo/history/evals.jsonl
/entiendo/baselines/*.json
/entiendo/baselines/*.pending.json
/entiendo/baselines/*.bless.json

# `ent serve` edit backups (L5) — runtime data.
/entiendo/.edit-backups/

# `ent serve` steering bridge (Phase C) — runtime data.
/entiendo/steering/
