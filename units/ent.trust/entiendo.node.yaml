# NOTE: entrypoint takes (root, node_id) — blocked on the single-arg harness seam
apiVersion: entiendo/v1
kind: Node
id: ent.trust
name: Baselines & Blessing
task: Hold eval baselines and bless records; blessing requires a resolvable human identity.
description: "The trust ledger under entiendo/baselines/: baseline stats, bless records with\
  \ a real blessedBy (\"unknown\" is unwritable), read/write with human confirmation gates.\
  \ AI drafts, humans bless \u2014 this unit is where that lands."
nodeKind: state
group: quality
owner: akashdatageek
status: experimental
claims:
- src/ent/baselines.py
- src/ent/identity.py
contract:
  invariants: []
  sideEffects: writes
evals:
  tier0:
  - type: invariant_check
observability:
  spanName: ent.trust
