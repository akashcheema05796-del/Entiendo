# NOTE: tools take (root, ...) — blocked on the single-arg harness seam
apiVersion: entiendo/v1
kind: Node
id: ent.bridge
name: The Bridge
task: 'Steer-edit-verdict loop: queue steering intents, apply boundary-confined edits, rerun
  tier0, post verdicts.'
description: "File-based steering queue with atomic claims, the edit review loop (boundary\
  \ + tier0 + blast radius + approval), the MCP server exposing the eleven tools, and the\
  \ SDK-backed edit model (degrades cleanly without credentials). Writes are confined to a\
  \ unit's claims \u2014 always."
nodeKind: compute
group: agents
owner: akashdatageek
status: experimental
claims:
- src/ent/steering.py
- src/ent/editloop.py
- src/ent/mcp_server.py
- src/ent/agent.py
contract:
  invariants: []
  sideEffects: writes
evals:
  tier0:
  - type: invariant_check
observability:
  spanName: ent.bridge
dependencies:
  calls:
  - ent.contracts
  - ent.evalkit
  - ent.graph
  - ent.packaging
  - ent.surface
  writes: []
